# Coordinator-handoff — AI Workspace control plane (2026-08-07, slutläge)

**Nästa session:** fortsätt härifrån. GitHub = source of truth. Operatören (Rikard)
kommunicerar på svenska. Delegera oberoende spår som parallella subagenter; presentera beslut
till operatören snarare än att bygga allt själv. **Denna handoff ersätter/efterföljer
`handoff-20260807.md`** (16:44) — läs denna först.

Repo: `C:\Users\rikar\Cortxt\projects\ai-workspace-control-plane`
Branch: `agent/separate-harness-verticals`

---

## Vad hänt denna session (övertagning 2026-08-07)

### Slutfört — Done-batch
- **#24 + sub-issues #26–#29** (schema/manifest-fixar, commit `8ef2644`, granskad Kimi 2.6)
  → **Workflow Status Done** + stängda.
- **#30 + sub-issues #31–#34** (discovery-run Steg A, mekanikbevis) → **Done** + stängda.
- Verifierat via GraphQL (inte bara rc): Workflow Status = Done på alla 10.

### Slutfört — #36 (Steg B, äkta domän-run) → Done
- Körde verkliga workflowen (`system-prompt-classify.md`, `system-prompt-obligations.md`) över
  de **12 syntetiska fixtures** med researcher-worker.
- **Resultat:** 12/12 riskklass-match, 24/24 schema-valid, AC3 (primary-source-marker) 0 violations,
  obligations-kontrakt (high_risk→4, prohibited→[], övriga→[]), `run-vertical-evals.py` **PASS 47/47, 12/12**.
- **Oberoende granskning (Kimi 2.6, Codex-substitut):** initialt "KRÄVER ÄNDRINGAR" pga två fynd.
  Fynd #2 ("eval-report inkonsistens") var en **falsk positiv** — granskaren tolkade
  `run-vertical-evals.py` som modelljämförelse, men det är **fixture-läget** (validerar fyxturens
  egen `expected_output`; läser aldrig classify/obligations-JSON). Korrigerat med evidens.
- **Omkalibrering efter operatörsbeslut:** BND-001/002 (avsiktligt `uncertain`) klassades om till
  `uncertain` via **Kimi 2.6 + kalibrerad gränsfalls-prompt** (free-modellen hängde på den längre
  prompten). Därefter **12/12 riskklass-match**.
- #36 → **Done** (verifierat), kommentarer postade (envelope, granskning+korrigering, omkalibrering).

### Nytt — #37 (följd-issue)
- **[v0.2] Uncertainty-kalibrering för gränsfalls-classificering** → öppnad, i Project 4, **Inbox**.
- Adresserar free-modellens överkonfidens på tvetydiga gränsfall (BND-001/002).
- Innehåller föreslagna åtgärder (model-assisted rubric, kalibrerad prompt-sektion,
  eval-assertion, modell-rutt) + AC + `Blocked by: #36`.

---

## Öppna arbetsuppgifter (inte klara)

| Issue | Innehåll | Status |
|---|---|---|
| **#37** | Uncertainty-kalibrering v0.2 (följd efter #36) | Inbox |
| #6, #7, #8, #14–19, #21, #22, #23 | Övrig backlog | Inbox |

(Gamla #24, #26–#34, #36 stängda/Done.)

---

## Nästa steg (rekommenderad ordning)
1. **#37** är naturligast (ny följd-issue, uncertainty-kalibrering) — presentera uppdelning till
   operatören innan bygge.
2. Eller ta nästa backlog-issue efter operatörens prioritering.
3. Ocommittad `README.md`-ändring (dokumentationsförbättring, +"Start here"+länkar) ligger kvar — **commit/push = operatör**.

---

## Regler som gäller (oförändrade)
- GitHub = master. Buzz = status/approval endast. Inga hemligheter/prompts/kunddata i git.
- Läs "Blocked by"+`Ready` INNAN dispatch. Ingen agent godkänner sitt eget arbete.
  Done/merge/push = operatör.
- Cost-first: minst modell; free-tier först. Kimi 2.6 för oberoende granskning (Codex-substitut).
  `hermes -p researcher --provider openrouter -m moonshotai/kimi-k2.6 -z "<prompt>"` (MÅSTE `--provider openrouter`).
- Verifiera själv innan du rapporterar ett subagent-resultat som "klart".
- Delegera oberoende spår parallellt (`delegate_task`, max 2 samtidigt, djup 1).

---

## Ladda upp i nästa session
- **Skills:** `manual-dispatch-adapter`, `hermes-kanban-task-management`, `code-review`,
  `to-tickets`, `handoff`.
- **Docs:** `docs/agents/current-operating-model.md`, `docs/architecture/dispatch-contract.md`,
  `docs/operations/manual-dispatch-routine.md`, `docs/wayfinder/handoffs/shared-context.md`.
- **Viktiga verifierade lärdomar (i `manual-dispatch-adapter`-skillen):**
  - Domän-run över N fixtures: **per-fixture**-dispatch, inte engångs-batch (free-modellen hänger).
  - Enkoda **paketkontraktet** i worker-prompten (obligations `[]` om ej high_risk).
  - Free-modellen **överkonfident på gränsfall** → gränsfall kan kräva Kimi 2.6.
  - `gh --body-file` kräver Windows-sökväg (`cygpath -w`).
