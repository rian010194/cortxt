# Agent Profiles

Status: scaffold

An Agent Profile is a versioned policy composition, not a separate runtime.

It selects:

- role and supported task classes;
- allowed reasoning strategies and operator set;
- tools and effect ceiling;
- memory policy;
- inference/model policy;
- verification policy;
- output contract.

Initial target profiles are `research-readonly-v1` and `coding-bounded-v1`.

