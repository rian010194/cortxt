# ---- Secrets & env ----
.env
.env.*
!.env.example
*.pem
*.key

# ---- Agent-local state (prompts, traces, reasoning — never commit) ----
.trace/
.hermes/
.kanban/
.codex-tmp/
.claude/
.remember/
archive/
*.trace
agent-platform/runs/
.tmp/

# ---- Internal working documents (archived locally, not published) ----
docs/superpowers/
docs/assessments/
docs/runbooks/
lab/

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ---- Node / builds ----
node_modules/
agent-platform/state/build/
*.tsbuildinfo

# ---- OS / editor junk ----
.DS_Store
Thumbs.db
desktop.ini
*.log

# ---- Accidental empty debris files/dirs ----
Appar
Avancerade

# ---- Lock/scratch ----
*.dispatch.lock
*.init.lock
.worktrees/
agent-platform-worktrees/

# ---- Local dispatch execution ledger (runs registry + claim store) ----
agent-platform/.dispatch/

# ---- Local session store (runtime.session_state records) ----
.sessions/

# ---- Runtime sync bookkeeping (theme_resolver.sync_widget_tokens) ----
agent-platform/widget/tokens.json.sync-marker.json

# ---- Rendered live widget snapshots (never tracked artifacts) ----
agent-platform/.widget-snapshots/
