# Vertical template

This directory reserves the package boundary; it is not an implemented
vertical. Add files only when a concrete experiment requires them.

A future package may contain `vertical.yaml`, workflows, schemas,
instructions, synthetic eval fixtures, and templates as described in the
[vertical package contract](../../docs/architecture/vertical-package-contract.md).
