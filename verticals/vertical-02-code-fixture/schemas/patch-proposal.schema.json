{
  "$id": "https://github.com/rian010194/cortxt/schemas/patch-proposal.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Model output for the fix-failing-test workflow: whole-file content, never a unified diff. The platform derives the diff itself. v0.1.0.",
  "version": "0.1.0",
  "type": "object",
  "additionalProperties": false,
  "required": ["changes", "rationale"],
  "properties": {
    "changes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "new_content"],
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Workspace-relative POSIX path to an EXISTING file. Absolute paths and traversal are refused by the platform's WriteGate."
          },
          "new_content": {
            "type": "string",
            "description": "The complete new text of the file."
          }
        }
      }
    },
    "rationale": { "type": "string", "minLength": 1 }
  }
}
